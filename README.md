# movie_ticket_project
# Initialize Git repository
git init

# Add project files
git add .

# Initial commit
git commit -m "Initial movie booking system"

# Add welcome message feature
git add .
git commit -m "Added welcome message"

# Add genre field feature
git add .
git commit -m "Added genre field to movies"

# Add seat map feature
git add .
git commit -m "Added seat map feature with view option"

# Add most booked movie feature
git add .
git commit -m "Added most booked movie feature"

# Add booking history feature
git add .
git commit -m "Added booking history feature"

# Update exit message
git add .
git commit -m "Updated exit message"

# Show commit history
git log --oneline
